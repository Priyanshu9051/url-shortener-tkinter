# url-shortener-tkinter
A simple Python GUI application built with Tkinter that converts long URLs into short URLs using the TinyURL service.
# URL Shortener using Python & Tkinter

This project is a simple **URL Shortener GUI application** built using **Python Tkinter**.  
It converts a long URL into a short URL using the **TinyURL service** via the `pyshorteners` library.

---

## 🚀 Features
- Simple and clean GUI
- Converts long URLs into short URLs
- Uses TinyURL API
- Beginner-friendly Python project

---

## 🛠️ Technologies Used
- Python
- Tkinter (GUI)
- pyshorteners library

---

## 📦 Requirements
Make sure you have Python installed, then install the required library:

```bash
pip install pyshorteners
pip install tkinter
